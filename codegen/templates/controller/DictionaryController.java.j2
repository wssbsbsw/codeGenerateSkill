package {{ package }}.controller;

import {{ package }}.common.Result;
import {{ package }}.dto.DictionaryOption;
import {{ package }}.service.DictionaryService;
{% if project.enable_swagger %}import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
{% endif %}import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
@RequestMapping("{{ project.api_prefix }}/system/dictionaries")
{% if project.enable_swagger %}@Api(tags = "Dictionary")
{% endif %}public class DictionaryController {

    private final DictionaryService dictionaryService;

    public DictionaryController(DictionaryService dictionaryService) {
        this.dictionaryService = dictionaryService;
    }

    @GetMapping("/{dictKey}/items")
{% if project.enable_swagger %}    @ApiOperation(value = "List enabled dictionary items by key")
{% endif %}    public Result<List<DictionaryOption>> listItems(@PathVariable("dictKey") String dictKey) {
        return Result.success(dictionaryService.listEnabledOptions(dictKey));
    }
}
